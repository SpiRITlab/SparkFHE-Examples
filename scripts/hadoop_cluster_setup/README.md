Note, run these scripts on the master node

# Install hadoop cluster
```
$> bash install_hadoop_cluster.bash MasterHostname,Worker1Hostname,Worker2Hostname,...
```

# Start hadoop cluster
```
$> bash start_cluster.bash
```